# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Achievement {
  createdAt: DateTime!
  description: String!
  id: String!
  imageIdentifier: String!
  isActive: Boolean!
  name: String!
  pointValue: Int!
  type: AchievementType!
  unlockCondition: String
  updatedAt: DateTime!
}

type AchievementPointsStatistics {
  completionPercentage: Int!
  earnedPoints: Int!
  possiblePoints: Int!
}

type AchievementStatistics {
  achievement: Achievement!
  unlockedByCount: Int!
  users: [AchievementUnlocker!]!
}

"""Types of achievements"""
enum AchievementType {
  consistency
  engagement
  legendary
  milestone
  social
}

type AchievementUnlocker {
  email: String!
  name: String!
  unlockedAt: DateTime
  userId: String!
}

type AchievementWithProgress {
  achievementId: String!
  currentProgress: Int!
  description: String!
  imageIdentifier: String!
  isUnlocked: Boolean!
  name: String!
  percentComplete: Int!
  pointValue: Int!
  targetProgress: Int!
  type: AchievementType!
  unlockedAt: DateTime
}

type AlgorithmSettings {
  createdAt: DateTime!
  id: String!
  organizationId: String!
  periodLengthDays: Int!
  randomSeed: Int!
  updatedAt: DateTime!
}

type AlgorithmSettingsResponse {
  createdAt: DateTime!
  id: String!
  organizationId: String!
  periodLengthDays: Int!
  randomSeed: Int!
  updatedAt: DateTime!
  warning: String
}

type AnonUser {
  email: String
  firstName: String
  hobbies: [Tag!]
  id: ID!
  interests: [Tag!]
  lastName: String
  organizationId: ID!
  preferredActivity: String
  profileImageUrl: String
  role: UserRoleEnum
}

input BanUserInput {
  expiresAt: DateTime
  reason: String!
  userId: ID!
}

type CalendarEvent {
  createdAt: DateTime!
  deletedAt: DateTime
  description: String
  endDateTime: DateTime!
  exceptionDates: String
  exceptionRrules: String
  externalId: String
  externalSource: String
  id: ID!
  rrule: String
  rruleRecurringId: String
  startDateTime: DateTime!
  title: String
  type: CalendarEventType!
  updatedAt: DateTime!
  userId: ID!
}

"""Type of calendar event: availability or unavailability"""
enum CalendarEventType {
  availability
  unavailability
}

input CancelMeetingEventInput {
  cancelledByUserId: ID!
  meetingId: ID!
  reason: String
}

input CreateCalendarEventInput {
  description: String
  endDateTime: DateTime!
  rrule: String
  startDateTime: DateTime!
  title: String
  type: CalendarEventType!
  userId: ID!
}

input CreateDepartmentInput {
  description: String
  name: String!
  organizationId: ID!
}

input CreateInviteCodeInputType {
  """Optional: hours until code expires (default: 30 days if not set)"""
  expiresInHours: Float

  """Optional: max uses for this code"""
  maxUses: Float
}

type CreateInviteCodeResponseType {
  code: String!
  expiresAt: DateTime
  inviteUrl: String!
  message: String!
  success: Boolean!
}

input CreateMeetingEventInput {
  createdByUserId: ID!
  endDateTime: DateTime!
  note: String
  pairingId: ID
  startDateTime: DateTime!
  userAId: ID!
  userBId: ID!
}

input CreateRatingInputType {
  feedback: String
  meetingEventId: ID!
  stars: Int!
}

type CurrentUser {
  about: String
  activeBan: UserBan
  department: Department
  departmentId: String
  email: String!
  firstName: String
  hobbies: [Tag!]
  id: ID!
  idealColleagueUsesRemaining: Int!
  interests: [Tag!]
  isActive: Boolean!
  lastName: String
  location: String
  organization: SimpleOrganizationType!
  organizationId: ID!
  position: String
  preferredActivity: String
  profileImageUrl: String
  profileStatus: ProfileStatusEnum!
  role: UserRoleEnum!
  supabaseUserId: String
  suspendedUntil: DateTime
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

input DeleteCalendarEventInput {
  id: ID!
  occurrenceStart: DateTime
  scope: String! = "this"
}

input DeleteDepartmentInput {
  id: ID!
  organizationId: ID!
}

type Department {
  createdAt: DateTime!
  description: String
  employeeCount: Float!
  id: ID!
  name: String!
  organizationId: ID!
  updatedAt: DateTime!
}

type DepartmentDistributionItemType {
  departmentName: String!
  userCount: Int!
}

type DepartmentDistributionResponseType {
  departments: [DepartmentDistributionItemType!]!
  totalUsers: Int!
}

type ExpandedCalendarEventOccurrence {
  id: ID!
  occurrenceEnd: DateTime!
  occurrenceStart: DateTime!
  originalEvent: CalendarEvent!
}

type GoogleCalendar {
  backgroundColor: String
  foregroundColor: String
  id: String!
  primary: Boolean
  summary: String!
}

type GoogleCalendarImportResult {
  importedCount: Float!
  message: String!
  success: Boolean!
}

input ImportGoogleCalendarEventsInput {
  accessToken: String
  calendarIds: [String!]!
  endDate: DateTime!
  startDate: DateTime!
}

type InviteCodeType {
  code: String!
  createdAt: DateTime!
  createdBy: User
  createdById: String!
  expiresAt: DateTime
  id: ID!
  inviteUrl: String!
  isActive: Boolean!
  maxUses: Float
  organizationId: String!
  usedCount: Float!
}

type InviteCodeValidationResponseType {
  expiresAt: DateTime
  isValid: Boolean!
  message: String!
  organizationId: String
  organizationName: String
  remainingUses: Float
}

input InviteUserInputType {
  email: String!
  organizationId: String!
}

type InviteUserResponseType {
  message: String!
  success: Boolean!
  userId: String
}

"""Meeting confirmation status: pending, confirmed, rejected, or proposed"""
enum MeetingConfirmationStatus {
  confirmed
  pending
  proposed
  rejected
}

type MeetingEvent {
  cancellationReason: String
  cancelledAt: DateTime
  cancelledByUserId: ID
  createdAt: DateTime!
  createdByUserId: ID!
  endDateTime: DateTime!
  id: ID!
  pairingId: ID
  startDateTime: DateTime!
  updatedAt: DateTime!
  userAConfirmationStatus: MeetingConfirmationStatus!
  userAId: ID!
  userANote: String
  userAProposedEndDateTime: DateTime
  userAProposedStartDateTime: DateTime
  userBConfirmationStatus: MeetingConfirmationStatus!
  userBId: ID!
  userBNote: String
  userBProposedEndDateTime: DateTime
  userBProposedStartDateTime: DateTime
}

type MessageModel {
  content: String!
  createdAt: DateTime!
  id: ID!
  isRead: Boolean!
  pairingId: ID!
  senderId: ID!
}

type MessagesReadEvent {
  pairingId: ID!
  userId: ID!
}

type Mutation {
  banUser(input: BanUserInput!): User!
  cancelMeeting(input: CancelMeetingEventInput!): MeetingEvent!
  confirmMeeting(meetingId: ID!, note: String): MeetingEvent!
  createCalendarEvent(input: CreateCalendarEventInput!): CalendarEvent!
  createDepartment(input: CreateDepartmentInput!): Department!
  createInviteCode(input: CreateInviteCodeInputType): CreateInviteCodeResponseType!
  createMeetingEvent(input: CreateMeetingEventInput!): MeetingEvent!

  """Rate a meeting you participated in"""
  createRating(input: CreateRatingInputType!): RatingType!
  deleteCalendarEvent(input: DeleteCalendarEventInput!): Boolean!
  deleteDepartment(input: DeleteDepartmentInput!): Boolean!
  deleteUser(id: ID!): User!
  executePairingAlgorithm(organizationId: String!): PairingExecutionResult!
  findIdealColleague: ID!
  importGoogleCalendarEvents(input: ImportGoogleCalendarEventsInput!): GoogleCalendarImportResult!
  inviteUserToOrganization(input: InviteUserInputType!): InviteUserResponseType!

  """Mark all messages in a pairing as read"""
  markMessagesAsRead(pairingId: ID!): Boolean!
  pauseActivity(input: PauseActivityInput!): CalendarEvent!
  proposeMeetingTime(input: UpdateMeetingEventConfirmationInput!): MeetingEvent!
  registerOrganization(input: RegisterOrganizationInputType!): RegisterOrganizationResponseType!
  rejectMeeting(meetingId: ID!, note: String): MeetingEvent!
  reportUser(input: ReportUserInput!): UserReport!
  resolveReport(input: ResolveReportInput!): UserReport!
  resumeActivity: Boolean!

  """Send a message to a user you are paired with"""
  sendMessage(input: SendMessageInput!): MessageModel!

  """Set typing status for a pairing"""
  setTypingStatus(isTyping: Boolean!, pairingId: ID!): Boolean!
  signUp(data: SignUpInputType!): User!
  unbanUser(userId: ID!): User!
  updateAlgorithmSettings(input: UpdateAlgorithmSettingsInput!): AlgorithmSettingsResponse!
  updateCalendarEvent(input: UpdateCalendarEventInput!, occurrenceStart: DateTime, scope: String! = "this"): [CalendarEvent!]!
  updateCurrentUserProfile(input: UpdateCurrentUserProfileInputType!): CurrentUser!
  updateDepartment(input: UpdateDepartmentInput!): Department!
  updateMeetingToProposedTime(meetingId: ID!): MeetingEvent!
  updateOrganization(input: UpdateOrganizationInputType!): OrganizationType!
  updateUser(data: UpdateUserInputType!): User!
}

type OrganizationType {
  address: String
  code: String!
  createdAt: DateTime!
  departments: [Department!]
  id: ID!
  imageUrl: String
  name: String!
  size: Float
  updatedAt: DateTime!
}

type PairingExecutionResult {
  message: String!
  pairingsCreated: Int!
  success: Boolean!
  unpairedUsers: Int
}

type PairingHistory {
  createdAt: DateTime!
  derivedStatus: PairingStatusEnum!
  id: ID!
  status: PairingStatusEnum!
  userA: User!
  userAId: ID!
  userB: User!
  userBId: ID!
}

type PairingPeriod {
  endDate: DateTime
  id: ID!
  organizationId: ID!
  startDate: DateTime!
  status: PairingPeriodStatusEnum!
}

enum PairingPeriodStatusEnum {
  active
  closed
  upcoming
}

type PairingStatusByUserType {
  count: Int!
  status: String!
  userEmail: String!
  userId: ID!
  userName: String
}

"""Pairing status"""
enum PairingStatusEnum {
  cancelled
  matched
  met
  not_met
  not_planned
  planned
  unspecified
}

type PairingStatusOverviewType {
  count: Int!
  status: String!
}

input PauseActivityInput {
  durationType: PauseDurationType!
  periodsCount: Float
  untilDate: DateTime
}

"""Type of activity pause duration"""
enum PauseDurationType {
  INDEFINITE
  N_PERIODS
  ONE_PERIOD
  UNTIL_DATE
}

"""Profile onboarding status"""
enum ProfileStatusEnum {
  active
  pending
  suspended
}

type Query {
  """Get all achievements with current user's unlock status and progress"""
  achievements: [AchievementWithProgress!]!
  activePairingPeriod: PairingPeriod

  """Get all available achievements"""
  allAchievements: [Achievement!]!
  allMeetingsForPairing(pairingId: ID!): [MeetingEvent!]!
  anonUsers: [User!]!
  calendarEventById(id: ID!): CalendarEvent
  calendarEventsByDateRange(endDate: DateTime!, startDate: DateTime!): [CalendarEvent!]!
  departmentDistribution: DepartmentDistributionResponseType!
  expandedCalendarOccurrences(endDate: DateTime!, startDate: DateTime!, userId: ID): [ExpandedCalendarEventOccurrence!]!
  getAlgorithmSettings(organizationId: ID!): AlgorithmSettings!
  getAllTags: [Tag!]!
  getCurrentUser: CurrentUser
  getDepartmentById(id: String!): Department
  getDepartmentsByOrganization(organizationId: String!): [Department!]!

  """Get all messages for a specific pairing"""
  getMessages(pairingId: ID!): [MessageModel!]!
  getOrganizationInvites: [InviteCodeType!]!
  getPairedUsers: [User!]!
  getPairingHistory: [PairingHistory!]!
  getTagsByCategory(category: TagCategory!): [Tag!]!
  getUserReceivedRatings(userId: ID!): UserReceivedRatingsType
  getUsersByDepartment(departmentId: String!): [AnonUser!]!
  googleCalendarList(accessToken: String): [GoogleCalendar!]!
  latestMeetingForPairing(pairingId: ID!): MeetingEvent
  meetingEventById(id: ID!): MeetingEvent
  meetingEventsByDateRange(endDate: DateTime!, startDate: DateTime!): [MeetingEvent!]!

  """Get all ratings for a specific meeting (admin only)"""
  meetingRatings(meetingEventId: ID!): [RatingType!]!
  myOrganization: OrganizationType

  """Get all ratings created by the current user"""
  myRatings: [RatingType!]!

  """Get organization-wide achievement statistics (org_admin only)"""
  organizationAchievementStatistics(organizationId: String!): [AchievementStatistics!]!
  organizationById(id: ID!): OrganizationType
  organizations: [OrganizationType!]!
  pendingMeetingConfirmations: [MeetingEvent!]!
  reportById(id: ID!): UserReport
  reports: [UserReport!]!
  statistics(filter: StatisticsFilterInputType): StatisticsResponseType!

  """Get meetings that can still be rated by the current user"""
  unratedMeetings: [UnratedMeetingType!]!
  upcomingMeetings: [MeetingEvent!]!

  """Get user's earned points from achievements"""
  userAchievementPoints(userId: String): AchievementPointsStatistics!

  """Get a specific user's unlocked achievements"""
  userAchievements(userId: String!): [UserAchievement!]!
  userById(id: ID!): User
  users(organizationId: ID): [User!]!
  validateInviteCode(code: String!): InviteCodeValidationResponseType!
}

type RatingType {
  createdAt: DateTime!
  feedback: String
  id: ID!
  meetingEvent: MeetingEvent!
  meetingEventId: ID!
  stars: Int!
  updatedAt: DateTime!
  userId: ID!
}

input RegisterOrganizationInputType {
  adminEmail: String!
  organizationAddress: String!
  organizationName: String!
  organizationSize: String!
}

type RegisterOrganizationResponseType {
  adminEmail: String!
  message: String!
  organization: OrganizationType!
}

enum ReportStatusEnum {
  pending
  resolved
}

input ReportUserInput {
  reason: String!
  reportedUserId: ID!
}

input ResolveReportInput {
  reportId: ID!
  resolutionNote: String
}

input SendMessageInput {
  content: String!
  pairingId: ID!
}

input SignUpInputType {
  email: String!
  firstName: String

  """Optional invite code for organization assignment"""
  inviteCode: String
  lastName: String
  password: String!
  profilePicture: Upload
}

type SimpleOrganizationType {
  code: String!
  id: ID!
  imageUrl: String
  name: String!
}

input StatisticsFilterInputType {
  endDate: String
  month: Int
  organizationId: String
  startDate: String
  year: Int
}

type StatisticsResponseType {
  inactiveUsersCount: Int!
  newUsersCount: Int!
  pairingsByStatus: [PairingStatusOverviewType!]!
  pairingsByStatusAndUser: [PairingStatusByUserType!]!
  reportsCount: Int!
}

type Subscription {
  """Subscribe to new messages in a pairing"""
  messageSent(pairingId: ID!): MessageModel!

  """Subscribe to read receipt events in a pairing"""
  messagesRead(pairingId: ID!, userId: ID!): MessagesReadEvent!

  """Subscribe to typing status changes in a pairing"""
  typingStatus(pairingId: ID!, userId: ID!): TypingStatus!
}

type Tag {
  category: TagCategory!
  createdAt: DateTime!
  id: ID!
  name: String!
  updatedAt: DateTime!
}

"""Category of a tag (hobby or interest)"""
enum TagCategory {
  HOBBY
  INTEREST
}

type TypingStatus {
  isTyping: Boolean!
  pairingId: ID!
  userId: ID!
}

type UnratedMeetingType {
  endDateTime: DateTime!
  id: ID!
  startDateTime: DateTime!
  userA: UserSummary!
  userAId: ID!
  userB: UserSummary!
  userBId: ID!
}

input UpdateAlgorithmSettingsInput {
  organizationId: String!
  periodLengthDays: Int
  randomSeed: Int
}

input UpdateCalendarEventInput {
  description: String
  endDateTime: DateTime
  id: ID!
  startDateTime: DateTime
  title: String
  type: CalendarEventType
}

input UpdateCurrentUserProfileInputType {
  about: String
  avatarUrl: String
  departmentId: String
  firstName: String
  hobbyIds: [String!]
  interestIds: [String!]
  lastName: String
  location: String
  position: String
  preferredActivity: String
}

input UpdateDepartmentInput {
  description: String
  id: ID!
  name: String!
}

input UpdateMeetingEventConfirmationInput {
  meetingId: ID!
  note: String
  proposedEndDateTime: DateTime
  proposedStartDateTime: DateTime
  status: MeetingConfirmationStatus!
  userId: ID!
}

input UpdateOrganizationInputType {
  address: String
  id: ID!
  imageUrl: String
  name: String
  size: Float
}

input UpdateUserInputType {
  about: String
  email: String
  firstName: String
  hobbies: String
  id: ID!
  interests: String
  lastName: String
  preferredActivity: String
  profileImageUrl: String
  role: UserRoleEnum
}

"""The `Upload` scalar type represents a file upload."""
scalar Upload

type User {
  about: String
  activeBan: UserBan
  createdAt: DateTime!
  department: Department
  departmentId: String
  email: String!
  firstName: String
  hobbies: [Tag!]
  id: ID!
  idealColleagueUsesRemaining: Int!
  interests: [Tag!]
  isActive: Boolean!
  lastName: String
  location: String
  organizationId: ID!
  position: String
  preferredActivity: String
  profileImageUrl: String
  profileStatus: ProfileStatusEnum!
  role: UserRoleEnum!
  supabaseUserId: String
  suspendedUntil: DateTime
  updatedAt: DateTime!
}

type UserAchievement {
  achievement: Achievement!
  createdAt: DateTime!
  currentProgress: Int!
  id: String!
  notificationSent: Boolean!
  unlockedAt: DateTime
  updatedAt: DateTime!
}

type UserBan {
  bannedById: ID!
  createdAt: DateTime!
  expiresAt: DateTime
  id: ID!
  organizationId: ID!
  reason: String!
  userId: ID!
}

type UserReceivedRatingsType {
  averageRating: Float
  ratings: [RatingType!]!
  totalRatings: Int!
  userId: ID!
}

type UserReport {
  createdAt: DateTime!
  id: ID!
  pairingId: ID!
  reason: String!
  reportedUser: User!
  reportedUserId: ID!
  reporter: User!
  reporterId: ID!
  resolutionNote: String
  resolvedAt: DateTime
  resolvedBy: User
  status: ReportStatusEnum!
}

"""User role"""
enum UserRoleEnum {
  org_admin
  super_admin
  user
}

type UserSummary {
  firstName: String
  id: ID!
  lastName: String
}